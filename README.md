# LocalLens
LocalLens is a smart housing price intelligence platform built for metropolitan cities. You open the map, pick a locality, and instantly get a data-driven price estimate for any property, not based on someone's opinion, but based on thousands of real transactions, processed by machine learning models trained specifically for each city.
